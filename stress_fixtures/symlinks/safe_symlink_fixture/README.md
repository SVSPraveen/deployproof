# Safe In-Repo Symlink Stress Fixture

This fixture demonstrates a legitimate in-repo symbolic link where the link destination resides inside the repository root. DeployProof verifies that the resolved path does not escape the workspace.
